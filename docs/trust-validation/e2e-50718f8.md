# Native Codex E2E — 50718f8

Date: 2026-09-05. **Completed with authorized recovery.** The unassisted run
blocked during shipping. Product checks, full review, archive validation, and
local integration ultimately passed. Installed plugin source stayed unchanged;
the candidate defects remain to be fixed.

## What this run proved

The owner requested an E2E of candidate
`50718f8100c04c1ff06e644930e0743d4a3950de`. The fixture used native Codex CLI
0.153.4, gpt-6-astra with high reasoning, quick lane, full review, no panel, and
an existing local origin. The parent evaluator reviewed and approved fixture
intent, plan, and shipping gates under the E2E request. These are evaluator
approvals, not fabricated human receipts. No new direct baseline was run.

- All six independent widget acceptance checks passed. Git object checks prove
  the delivered script and tests are identical to the verified task landing.
- T001 landed at `56c94e440b328c5ecd3c9e7be73fa00ef8474ec7` after authoritative
  isolated verification. Its tests first failed against the original script;
  the task log preserves test-strength evidence.
- Full wave review passed. Pre-created task verification and fresh wave-review
  collection worked, avoiding the earlier run's build failures.
- Final review passed all six success criteria. Project Verify passed once at
  the original reviewed commit and again at the bookkeeping correction commit.
  The correction changed HEAD, and the ship contract required evidence there;
  the repeated work was recovery cost, not additional product scope.
- Canonical archive preflight, ship validation, and `validate-integrated` passed.
- The ship commit is `c3195a67d43eb08e0c359975c83ec20a3bc23cf4`.
- Local integration is `c8065c3e69c244454198f1226b668f8ded9c4e46`, a two-parent
  merge with the ship commit as its second parent. The exact remote-tracking
  ref and the bare origin's main ref agree.
- Annotated tag `milestone/001-widget-counter` points to that integration.
  The primary worktree is clean, no sidecars remain, and the final Git bundle
  passed verification. No external publication or next milestone occurred.

## Defects and required intervention

1. **Ship command contract mismatch.** The canonical ship skill specifies
   `--project-dir <absolute .project>`, but `build_state.py verify-landed`
   requires a repository-relative path. The evaluator selected `.project`
   under the owner's continuation request; installed instructions were not edited.
2. **Unsupported bookkeeping location.** The native parent created and committed
   `.project/EXECUTION.md`. Archive generation rejects this root file, and ship
   validation also rejects moving or deleting it in the ship commit. The owner
   authorized external evidence preservation, canonical archive undo, and a
   separate bookkeeping correction commit (`a60a3ec`).
3. **Inconsistent review parsing.** FINAL.md repeated an identical
   `Reviewed HEAD:` field. The final gate accepted it, but archive undo rejected
   it. The owner approved the exact one-line correction to the prepared artifact.
4. **Recovery rejects state history.** Four appended state events from the
   recorded stop/resume steps prevented undo's exact ownership check. The owner
   approved restoring the expected prepared state text; the full original log
   remains in the external snapshot.

The native parent also used an incorrect initial binding event, omitted a
required ship argument, continued a command batch after failure, and prematurely
claimed a corrected command passed. The execution record explicitly retracts
that claim. Recovery attempted one helper from a bundle that did not contain it,
then located the correct undo bundle. All failures are retained.

The testing observer initially counted only the latest invocation of a reused
reviewer thread. Final accounting now sums native task turns, retaining both
final reviews (3,612 and 3,153 output tokens). This was an evaluator accounting
correction, not a plugin source change.

## Cost

Total native output: **54,636 tokens**. CLI execution time: **2,308.14 seconds
(38.47 minutes)**, excluding evaluator pauses and owner response time.

| Work | Output tokens |
|---|---:|
| Parent orchestration, seven invocations | 34,553 |
| Codebase inspection | 3,327 |
| Docs inspection | 2,743 |
| Coder task, including tests | 4,226 |
| Wave review | 3,022 |
| Final review, two invocations | 6,765 |

The coder task used 7.7% of native output. The remaining 92.3% covers orchestration,
inspection, review, and recovery; it must not all be labeled verification.
Reasoning is already included in output counts. Native input and cached input
counts are available in result.json. Evaluator tokens are not included.

The owner corrected the testing policy verbatim: "no limit set for testing -
need to continue". No token limit applied to the continuation. Earlier counters
and overruns remain historical evidence; they were not reset or erased. The
CLI exposes observed usage rather than hard in-flight enforcement.

## What to fix next

Correct the canonical ship command, reject unsupported artifact locations before
archiving, and make final-review and recovery validation agree. These are
reproduced workflow gaps. Source changes and a fresh unassisted E2E are still
needed to prove they are fixed. This successful recovery does not establish
optimality or all-host release qualification.

## Evidence

- [Result and per-turn usage](evidence/releases/1.0.0/codex-e2e-50718f8/result.json)
- [Git integration proof](evidence/releases/1.0.0/codex-e2e-50718f8/git-proof.json)
- [Canonical command receipts](evidence/releases/1.0.0/codex-e2e-50718f8/canonical-receipts.json)
- [Final review](evidence/releases/1.0.0/codex-e2e-50718f8/FINAL.md)
- [Archive manifest](evidence/releases/1.0.0/codex-e2e-50718f8/MANIFEST.md)
- [Project Verify](evidence/releases/1.0.0/codex-e2e-50718f8/final-gap-1.md)
- [Verification ledger](evidence/releases/1.0.0/codex-e2e-50718f8/verify-ledger.jsonl)
- [Task and test evidence](evidence/releases/1.0.0/codex-e2e-50718f8/T001-widget-counter.md)
- [Failed commands](evidence/releases/1.0.0/codex-e2e-50718f8/failed-commands.json)
- [Original execution record](evidence/releases/1.0.0/codex-e2e-50718f8/EXECUTION.md)
- [Recovery execution](evidence/releases/1.0.0/codex-e2e-50718f8/RECOVERY-RUN.md)
- [Approved metadata patch](evidence/releases/1.0.0/codex-e2e-50718f8/metadata-recovery.patch)

Raw fixture, prompts, CLI events, complete recovery snapshots, and verified
bundles: `/Users/jeremymcspadden/orca/evaluations/gsd-path-e2e-50718f8`.
Native parent thread: `01a07163-750c-7e20-a515-07cdb33435b4`.
