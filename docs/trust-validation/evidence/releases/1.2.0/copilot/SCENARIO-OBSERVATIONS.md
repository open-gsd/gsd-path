# Copilot retry4 observations

Candidate: f36f24aed82ac0019d082c7e31b8a4342d8e03f1; package1.2.0.

Live receipt validation passed. Independent widget oracle passed all six checks. Native full-wave review was reused as final; repeated prepare-final at the same HEAD reused verification and left the two-entry ledger byte-identical. Native reviewer ran only direct CLI walkthroughs, read recorded isolated task Verify and inspected test source; it did not rerun tests. Coder native session7f87d819-badb-42d2-92f5-5dfed6afcf07 and actual isolate-verify output prove branch/worktree identity. All 18 native parent sessions, including children/resumes, were below30,000 output tokens; maximum21,937. The4,000 threshold is a warning. usage.json retains native shutdown accounting.

## Disclosed recovery deviation

A native parent called check_handoffs wave with absolute --review, received `handoff validation failed: wave review must be the canonical path .project/review/wave-1.cycle1.md`, then corrected the argument and passed the same canonical validator before running the required diagnosis. This was a diagnosis-ordering deviation. The next fresh native context ran canonical pipeline_diagnose before ship preparation: all seven probes passed with no findings. No state/artifact repair was used. Root accepted this recovered nonmutating invocation error as not leaving the requested release claims unproven; this is not a claim of flawless protocol compliance. Exact failed/corrected calls remain in review-path-negative.json; recovery-and-reuse.json contains diagnosis and later reuse evidence.

Earlier preapproval negative gates are retained in the raw fixture: docs auditor supplied primary Repo root instead of its sidecar; documented native header-only redispatch fixed it. A draft PLAN surface rename conflicted with approved INTENT; native planner diagnosed/stopped, then restored the exact approved name before approval. JSON shape and budget wording were corrected by the native define owner before approval. No evaluator edited protected state or reviews.

## Prior attempts

Prior retry2's extra reviewer test execution and budget overruns remain preserved under ../notes/copilot-history/retry2/. Retry3's cancelled stalled request and later budget overrun remain in the separate copilot-retry3 fixture. They are not relabeled as passing evidence.

Raw current fixture: /Users/jeremymcspadden/orca/evaluations/gsd-path-release-1.2.0-f36f24a/copilot-retry4. Native quick/run-*/events.jsonl and session-state records are preserved.
