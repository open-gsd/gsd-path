# OpenCode 1.2.0 — accepted retry4 evidence

Candidate f36f24aed82ac0019d082c7e31b8a4342d8e03f1; OpenCode1.18.25; fixture `/Users/jeremymcspadden/orca/evaluations/gsd-path-release-1.2.0-f36f24a/opencode-retry4/quick/repo`.
Strict host receipt assembly and `_validate_receipt` passed: [receipt-validation.json](receipt-validation.json). Independent product oracle:6/6PASS [product.json](product.json). This is a successful recovered run, not a claim that every attempt was flawless.

## Identity and execution

- Canonical activation recorded `build_t001`; native readback passed before dispatch and after landing. Task/manifest/receipt retain that exact normalized identifier.
- Original product-author child: `ses_f3f2e9f03ffexjo8nwW6uSruiN`, parent `ses_f3f3060c5ffeCipwGk2UY5OssM`. It wrote the owned implementation/tests and passed9own tests; its parent omitted mandatory model-policy resolution. That omission is preserved and is not retroactively authorized.
- Accepted replacement child: `ses_f3f20a175ffeS16bu9ErKFHSMU`, parent `ses_f3f2180c5ffet7nswg6lKp3njW`, call `call_51ae4fa48c5748aa84c5edbb`. A real ready policy record preceded dispatch. It independently read/adopted the existing owned patch, appended its own task Log result, and completed; canonical release followed. No tests or product code were rewritten to manufacture acceptance. The receipt binds this actual replacement completion; original authorship is retained above.
- Canonical isolated Task Verify9/9 passed, then landing `53096ee11c7226d4cacf98efd78756996145eb86`. Actual native Verify branch/path match the receipt exactly; [provenance-audit.json](provenance-audit.json) includes the observed helper result and bundle refs.
- Native reviewer `ses_f3f175bb7ffeq2VAQ3VkEgdLbw`, parent `ses_f3f185d2affenBemGsKrh9tsg0`, passed full Wave1 at `18b089c3cf7ac86a3db79d04e8d09bbff5aa5fdf` after a ready policy record. It ran8real CLI walkthroughs, used recorded Verify for SC6, and wrote the review itself in the canonical sidecar. Parent and child directory bindings were recorded against that sidecar.

## Reuse and shipment

First prepare-final reused the full-wave final-scope review and ran project Verify9/9 once. Repeating at unchanged HEAD `f5e2a76627f1d321530650e129edcbf7e42865a4` reused both final review and project Verify. No review_final child. Ledger unchanged:2entries,2556bytes, SHA256 `27c87cb9d7f91d977e1f212ab87d88d6620368efbbc4fe26a6af519325343d11`. [reuse-proof.json](reuse-proof.json) preserves actual native commands/results.

Fresh Git-only guard probes passed through the official manifest assembler. Ship `29dd1cff88fae67faacdf9283b058e8604b5fb40`, local-origin integration `85998c18bb2673e0b4b596164a7845865d2aa4e1`, annotated tag `milestone/001-count-cli`; native archive and integration validators passed. No external publication. All7fixture refs are in the bundle. Exact successful command strings and validator stdout are preserved in [receipt-command-provenance.json](receipt-command-provenance.json).

## Candidate provenance and budgets

[runtime-provenance.json](runtime-provenance.json):63local/runtime Python helper files match the frozen candidate byte-for-byte;286native read/bash/skill/write/edit actions audited across parents and children; no native skill-tool/global installed skill-resource calls. Runtime digest `7651c2147fa0e398eda0d7de3fc64bcbbb55139a4f145f03fe8c896f9c91f334`. In particular shipment used that runtime's archive helper. Raw unedited logs remain in the evaluation directory; [raw-log-index.json](raw-log-index.json) records paths, sizes and hashes.

[budget-evidence.json](budget-evidence.json) sums OpenCode's exclusive output+reasoning counters across each parent and its native children/resumes. Accepted context totals:19939,16644,16161,12096,6010,17529,12192,7189,6101; maximum19939 under owner30000. The4000 threshold is a warning.

Both original coder and canceled recovery are excluded from accepted budget evidence. Their connected group has recorded21504 tokens plus an unknown provider usage tail in two unfinished messages; it is NOT labeled budget-pass. Fresh replacement6010 is a distinct accepted lineage, not a reset or relabeling of that canceled group. Raw counters and unfinished parts remain preserved.

## Preserved negative evidence

- Inspection auditor omitted the required empty User rulings table. Canonical diagnosis ran, the original native auditor corrected its own artifact, then finish-inspect passed.
- Still-unapproved Define draft grouped two criteria and left the test criterion unnumbered. Evaluator supplied the exact six-criterion correction at the legal Define gate; native Define recorded it before approval/plan.
- Original coder parent omitted model-policy resolution. Recovery ran diagnosis and real resolve, then attempted same-child resume. Three native reads stalled on external_directory permission asks: old native session directory was SOURCE despite fixture subprocess cwd. Evaluator canceled the runner (exit130), confirmed no live runner, retained orphaned DB parts without edits, and used a fresh policy-ready child in the same fixture. Fresh parent/child directory binding was measured as the fixture; no global permission changes. The inherited PWD/project-binding mechanism is a supported explanation, not independently proven host internals.
- Review preparation first omitted capabilities from model resolve; blocked result is preserved. Corrected resolve returned ready. Its immediate diagnosis was omitted, so the next context first ran canonical diagnosis before reviewer dispatch. No parent review edits or manual state/sidecar repairs.

Proofs for these recoveries: [retry4 history](../notes/opencode-history/retry4/). Prior retry3 exact blocked receipt remains [blocked-receipt.md](../notes/opencode-history/retry3/blocked-receipt.md). Prior retry2 global-helper provenance failure and earlier fixtures are preserved, not reused as passing evidence.
