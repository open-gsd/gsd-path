# Continued release evaluation

Owner ruling, verbatim: "run till all pass". This authorizes continued diagnosed attempts beyond the previous three-round stop. It does not waive proof, budget, or pipeline contracts.

Candidate remains f36f24aed82ac0019d082c7e31b8a4342d8e03f1, package1.2.0. Qwen/Kiro/Zed remain excluded. No external publication is authorized by this evaluation.

Acceptance: all eight required structural receipts validate against the frozen candidate; each has native child/landing/archive/integration proof and independent product acceptance; originally requested full-wave final reuse and same-HEAD reuse pass; no unwaived protocol or session-budget failure in the accepted attempt. Historical failed attempts stay visible.

Codex and Cursor already prove these claims. Fresh attempts are needed for Claude (full-wave final reuse), Copilot (protocol and budget), Kimi (budget), Grok (budget and unavailable reviewer), OpenCode (invalid ship recovery), and Antigravity (evaluator manifest error and budget/protocol failures).

Attempts: antigravity-retry3, opencode-retry2, grok-retry2, copilot-retry3, kimi-retry2, then claude-retry2. All native logs/fixtures are under /Users/jeremymcspadden/orca/evaluations/gsd-path-release-1.2.0-f36f24a. Split phase contexts and documented lower effort reduce repeated source reads; actual helper results remain authoritative. Read observed isolation branch/path before constructing each manifest.

Antigravity retry3 inspection completed, but native parent usage11,170 plus childusage11,083+8,149 totaled30,402. The parent CLI result excluded children. Native SQLite gen_metadata counters were decoded and calibrated against the exact CLI parent output/input/cache totals, including an earlier high-effort run. Retry3 is preserved as a budget failure; retry4 splits setup, each inspector, and collection into separate contexts.

Continuation checkpoint: Grok retry2 preserved a final-reuse rejection caused by an evaluator surface-name mismatch; retry3 uses the exact canonical name. Copilot retry3 preserved a 31,849-token correction context; retry4 splits each inspection assignment and collection. Antigravity retry4 passed inspection after resuming the original native children; setup/mapper/docs groups used 4,241/10,126/12,145 output tokens. Persistent native input keeps later child-dispatch parents alive. OpenCode retry2 passed task landing and full wave review; closed inspect/define/plan/build/wave contexts used 23,918/10,504/16,927/24,117/27,004 generated tokens (output plus separate reasoning counters, including children). These are progress receipts, not release approval.

Antigravity retry4 is now complete: receipt validates with native child/landing/archive/integration and Git guard proof, independent oracle6/6, full-wave and repeated verification reuse, exactly2 ledger entries, maximum native parent+children output16,553. Earlier Antigravity failures remain in history. OpenCode retry2 is preserved as failed candidate provenance: shipment invoked an older global archive helper despite local candidate installation; no accepted receipt issued. Retry3 pins literal local helper paths and has passed native inspection within23,419 generated tokens.

Latest owner correction: "Yes — use affected checks (recommended)". Finish the
current native attempts, then use validated receipts and affected checks for
routine releases. Do not start another full fixture retry after a current failure.
Copilot retry4 now passes receipt, oracle6/6, full-wave and repeated verification
reuse; maximum native context21,937 output tokens. Its recovered path error and
delayed diagnosis are disclosed in copilot/SCENARIO-OBSERVATIONS.md.
