# Claude existing-fixture recovery accepted

Candidate `f36f24aed82ac0019d082c7e31b8a4342d8e03f1`, package `1.2.0`, Claude Code `2.1.278`. The owner said **"fix them"**, authorizing correction of the blockers. This correction reused the completed installation, inspection, definition, plan, native coder, isolated task Verify and landing in `/Users/jeremymcspadden/orca/evaluations/gsd-path-release-1.2.0-f36f24a/claude-retry2`. It did not repeat setup or build.

The current [receipt](../claude.md) passed the assembler's strict `_validate_receipt`; [exact result](claude-history/recovery-cycle2/receipt-validation.json). The independent [CLI oracle](claude-history/recovery-cycle2/oracle.json) passed all six cases. Native archive validation and local bare-origin integration passed. No external publication occurred.

The former 32,066-token review context remains **failed and superseded**. It is excluded from the accepted review lineage. Its [budget record](claude-retry2-budget.json), [blocked receipt](claude-history/retry2/blocked-receipt.md), original raw logs and [unchanged cycle1 artifact](claude-history/retry2/wave-1.cycle1.md) remain intact. Cycle1 SHA256 is `ff51884b5c1ada97ba33b2e978b453a4f308fc584dac21bee07813a95b17d939`. Canonical collection and checkpoint `f50e83c69c7dedaece13f67e657249e2f7a281dc` preserved it solely as required contiguous history; nobody rewrote its verdict or evidence.

A new native cycle2 reviewer used the pinned `dispatch_driver.py review` child-command contract. The real CLI invocation retained native hooks and used model-policy-recorded `--effort medium`, model `inherit`. Logical identity `review_wave_1_cycle_2`; native child session `824cd1b8-6514-4ade-87c5-462a525d8279`; launch parent `7d74e63b-73bb-4e76-bed2-327e235fdf3a`. It read the isolated task diff and recorded 8-test Verify, independently walked the real CLI, and authored full final scope for SC1–SC6. It did not rerun task/project Verify. [Driver identity and selection](claude-history/recovery-cycle2/review-driver-state.json), [native result](claude-history/recovery-cycle2/review-native-result.json), [raw file hashes](claude-history/recovery-cycle2/raw-log-hashes.json).

Accepted proof:

- Actual coder `build_t001`, native Agent task `adf313b0fbce75094`, parent `b5627f8a-3c74-4064-aa69-7d477b67e7c7`; native stream `quick/run-20260920T185113013151Z/events.jsonl`. Landing `426bd9f0447406d0a96d97815908cb277c3fa194`; observed Verify branch `gsd-path-verify/task-t001-verify`.
- Cycle2 hash `ed98dd873db2b05c2b2ad6b6d5d154fc97a0b3817913da767aa0fea782d86860`; canonical review checkpoint `655bc512801b8facb11d529a1b80b68943bd22b8`.
- FINAL was generated from `.project/review/wave-1.cycle2.md` at `49e92866f539ae62e917c54612bac69dd872245d`. The project Verify ran once: 8 tests passed. Same-HEAD prepare-final repeated with both `final.reused` and `verification.reused` true. The ledger stayed at 2 entries, SHA256 `f460b5c34025bbb0938d38ecd4c18fb9e768431703442b49277175edee588eb8`.
- Ship `a0ea41d4e5c6215511480b51d888ce1a25cba307`; integration `4182eade3a40a5648190be102e5a51531dad43ae`; annotated tag `milestone/001-count-cli-json`. Primary is clean with only its own worktree remaining. Manifest and receipt name cycle2 and the actual coder; bundle contains the same committed archive and heads.
- Fresh native archive-edit guard probe from this fixture and executable Git-hook checks passed. Prior release evidence was not substituted.

[Budget accounting](claude-history/recovery-cycle2/budget.json) explicitly maps retained valid contexts and replacement contexts. It deduplicates actual native assistant message IDs, sums output tokens across parent, children and resumed turns, and cross-checks terminal counters. The direct CLI child has a separate native directory and is explicitly added to its launch parent.

| Accepted correction context | Output tokens |
| --- | ---: |
| Cycle2 launch parent + direct native reviewer | 3,256 + 17,689 = **20,945** |
| Collection and both final reuse checks | 2,722 |
| Archive preparation | 461 |
| Shipment plus resumed diagnosis | 5,516 + 816 = **6,332** |

Every accepted context is below the owner's 30,000 aggregate session limit. The 4,000 task level is a warning; warning contexts are listed. This does **not** mean every historical session passed: the original 32,066 context remains failed.

Recovered negatives and limits:

- Reviewer read commands initially hit zsh `==INTENT not found` and macOS `cat: illegal option -- A`; corrected reads succeeded. Its quoted shell heredoc was rejected by the native guard before writing. The reviewer then used the native Write tool. The parent did not edit its artifact.
- Shipment parent added `; echo "EXIT=$?"` to `record-shipment`. The native archive guard refused that compound command before the helper ran. The plain pinned helper invocation succeeded. The host inspected guard source and continued before running canonical diagnosis. This ordering deviation is preserved; it is not a flawless protocol claim. A resumed native [delayed diagnosis](claude-history/recovery-cycle2/delayed-diagnosis-run.json), after integration, returned `status: ok`, no findings, seven passing probes, no open journals and verified completion. No manual state/archive repair occurred.
- Prior docs-token misclassification, corrected definition wording and the earlier first-run Surface mismatch remain in the historical notes. Receipt acceptance does not relabel them as passing attempts.

[Exact command provenance](claude-history/recovery-cycle2/command-provenance.json) records native tool IDs and full inputs. The helper returns no integration worktree path, so that retired path is explicitly derived through the pinned `integration_names(pin=False)` and retained workspace pin, rather than represented as printed native output.
