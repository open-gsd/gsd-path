# Review — wave 1, cycle 1

Reviewed HEAD: 52bca9fe60bbdb72023c752470bb2d5366ed2dbf
Review scope: final

Wave verdict: pass
Cycle: 1
Depth: full
Tasks reviewed: 1

## T001 — Add opt-in --json output and guarded integer parsing to count.py with real-CLI tests: pass

- ✅ `python3 count.py` prints exactly `0 widgets` to stdout and exits 0, unchanged from today. — isolated landing 426bd9f0447406d0a96d97815908cb277c3fa194 on base eb0cc8fc866e068b14721b288a2a6be263728c22: `count.py:5-8,13` sets `raw = rest[0] if rest else "0"` and falls through to `f"{n} widgets"` when `--json` is absent; that f-string on `count.py:13` is byte-identical to the pre-task `count.py:3`. Walked in the sidecar at the reviewed HEAD: `python3 count.py` printed `0 widgets` with exit 0, and `od -c` confirmed the exact bytes `0 w i d g e t s \n`. Recorded isolated Verify in the task Log covers it as `test_no_argument_prints_zero_widgets (__main__.TextOutput) ... ok`.
- ✅ `python3 count.py 7` prints exactly `7 widgets` and exits 0, and `python3 count.py -5` prints exactly `-5 widgets` and exits 0 — signed integers keep the current text output with no pluralization or range change. — `count.py:8-13` parses with a bare `int(raw)` and prints `f"{n} widgets"` with no pluralization branch and no range test, so `-5` survives `int()` and reprints unchanged. Walked at the reviewed HEAD: `python3 count.py 7` printed `7 widgets` exit 0 and `python3 count.py -5` printed `-5 widgets` exit 0. Recorded isolated Verify: `test_positive_count (__main__.TextOutput) ... ok` and `test_negative_count (__main__.TextOutput) ... ok`.
- ✅ `python3 count.py --json` prints exactly one JSON object, `{"widgets": 0}`, to stdout on its own and exits 0; the `widgets` value parses as a JSON integer, not a string, and no human text line accompanies it. — `count.py:6,13` sets `as_json = "--json" in args` and emits `json.dumps({"widgets": n})` from the script's single `print` statement, so no text line can accompany the object; `n` comes from `int(raw)` on `count.py:9`, so the serialized value is a JSON integer. Walked at the reviewed HEAD: `python3 count.py --json` printed `{"widgets": 0}` exit 0, and `od -c` confirmed the exact bytes `{ " w i d g e t s " : 0 } \n` with nothing else on stdout. Recorded isolated Verify: `test_json_default_is_single_object_with_integer_zero (__main__.JsonOutput) ... ok` and `test_json_output_carries_no_human_text_line (__main__.JsonOutput) ... ok`; `test_count.py:47-51` asserts one stdout line, payload equality with `{"widgets": 0}`, `assertIsInstance(payload["widgets"], int)`, and `assertNotIsInstance(payload["widgets"], bool)`.
- ✅ `python3 count.py --json 7` and `python3 count.py 7 --json` each print exactly one JSON object whose `widgets` value is the integer `7`, exit 0, and produce identical stdout. — `count.py:6-7` tests `"--json" in args` and rebuilds `rest` by filtering every `--json` token out of `sys.argv[1:]`, so flag position cannot reach the count read on `count.py:8`; the naive `sys.argv[1]` trap named in INTENT.md Risks is absent. Walked at the reviewed HEAD: both orders printed `{"widgets": 7}` with exit 0, and piping the first into `cmp -s` against the second reported `identical`. Recorded isolated Verify: `test_json_flag_before_and_after_count_agree (__main__.JsonOutput) ... ok`; `test_count.py:53-61` asserts both payloads equal `{"widgets": 7}`, integer type, and `before.stdout == after.stdout`.
- ✅ `python3 count.py abc` writes a message naming `abc` to stderr, writes nothing to stdout, exits nonzero, and prints no Python traceback; `python3 count.py --json abc` fails the same way, with no partial or malformed JSON object on stdout. — `count.py:8-12` parses inside `try`/`except ValueError` before any `print`, so the single stdout write on `count.py:13` is unreachable on bad input in either mode; the handler writes `count.py: not an integer: abc` to `sys.stderr` and calls `sys.exit(2)`, which suppresses the traceback. Walked at the reviewed HEAD: `python3 count.py abc` gave exit 2, empty stdout, stderr `count.py: not an integer: abc`; `python3 count.py --json abc` gave the identical exit 2, empty stdout, the same stderr line, and no partial JSON. Recorded isolated Verify: `test_text_mode_rejects_non_integer (__main__.BadArgument) ... ok` and `test_json_mode_rejects_non_integer_without_partial_json (__main__.BadArgument) ... ok`; `test_count.py:67-72` asserts nonzero return, empty stdout, `abc` present in stderr, and `Traceback` absent.
- ✅ `test_count.py` is a stdlib-only `unittest` module that launches the real `count.py` CLI through `subprocess` and asserts criteria 1–5 against actual stdout, stderr, and exit status; running `python3 test_count.py -v` reports every test passing and exits 0. — `test_count.py:2-6` imports only `json`, `os`, `subprocess`, `sys`, `unittest`; `test_count.py:11-17` runs `subprocess.run([sys.executable, SCRIPT] + list(args), ...)` against the real script resolved from `__file__` on `test_count.py:8`, not an imported function; `test_count.py:84-85` is the `__main__` guard calling `unittest.main()`. The three classes map to criteria 1–2 (TextOutput), 3–4 (JsonOutput), and 5 (BadArgument), asserting `r.stdout`, `r.stderr`, and `r.returncode`. The orchestrator's recorded isolated Verify in the task Log (sidecar `gsd-path-verify/task-t001-verify`) reports pass, exit 0, all eight tests `ok`, ending `Ran 8 tests in 0.227s` / `OK`.
- ✅ None — the task's Interface contract section is literally `None`, and the wave holds one task with `deps: []`, so no shape is exchanged with another task. The landing patch adds only `count.py` (importing stdlib `json` and `sys`) and the new `test_count.py` (stdlib only); nothing in it imports from, or is imported by, another task's file, so there is no cross-task interface to violate.

Warnings (non-blocking):
- Weak assertion: `test_count.py:63-64` (`test_json_output_carries_no_human_text_line`) strips `"` characters from stdout and asserts `widgets` followed by a newline is absent. Because the JSON object always ends in `}` before its newline, that assertion can never fail for any JSON payload, so it adds no real protection. The criterion it targets is genuinely proven by `test_json_default_is_single_object_with_integer_zero` (`test_count.py:47-51`, exact payload equality plus a single-line assertion) and by the walkthrough `od -c` byte dump, so this is not blocking. Tighten by asserting full stdout equals the serialized object plus one newline.
- Contract silence, not a defect: `count.py:7` filters out every `--json` token anywhere in `sys.argv[1:]`, so a literal `--json` can never be read as the positional count and a repeated `--json` is absorbed silently. No task criterion or INTENT success criterion governs those inputs, so this is recorded for a human eye rather than judged.
- Exit status `2` on bad input (`count.py:12`) is not pinned by any criterion — SC5 and AC5 require only nonzero — so a later change to that number would fail no test.

Contract violations (blocking):
- None. `git show --stat 426bd9f0447406d0a96d97815908cb277c3fa194` changes exactly three paths: `count.py` and `test_count.py`, both declared in the task's `files`, plus its own task file. The task-file hunk touches only orchestrator-owned frontmatter (`status`, `agent`, `base`) and appends two Log entries; no contract-body line (Context, Approach, Interface contract, Intent coverage, Acceptance criteria, Verify) changed. No `.gsd-path/**`, `.claude/skills/**`, `AGENTS.md`, or `WORKFLOW.md` path appears in the commit, so the INTENT.md installed-pipeline veto holds, and no third-party import, manifest, runner, lint, or CI file was added, so the stdlib-only and no-CI/package vetoes hold.

## Intent coverage

### SC1 — `python3 count.py` prints exactly `0 widgets` to stdout and exits 0 — unchanged from today.: pass

- **Surface**: count.py command line
- **Check**: `python3 count.py` with its exit status echoed, then `python3 count.py | od -c`, both run in the review sidecar worktree at 52bca9fe60bbdb72023c752470bb2d5366ed2dbf (PLAN.md Surface contract walkthrough step 1)
- **Observed**: stdout `0 widgets`, exit status 0; `od -c` shows `0   w   i   d   g   e   t   s  \n` and nothing else. Interpreter Python 3.9.6.
- ✅ Walkthrough step 1 executed at the reviewed HEAD produced exactly `0 widgets` with exit 0 and no extra bytes. `count.py:13` still emits the original `f"{n} widgets"` on the no-flag path and `count.py:8` defaults `raw` to `"0"` when no argument is given, so the default text invocation is byte-for-byte unchanged from `count.py:3` at base eb0cc8fc866e068b14721b288a2a6be263728c22.

### SC2 — `python3 count.py 7` prints exactly `7 widgets` and exits 0; `python3 count.py -5` prints exactly `-5 widgets` and exits 0 — signed integers keep their current text output, with no pluralization or range changes.: pass

- **Surface**: count.py command line
- **Check**: `python3 count.py 7` and `python3 count.py -5`, each with its exit status echoed, run in the review sidecar worktree at 52bca9fe60bbdb72023c752470bb2d5366ed2dbf (PLAN.md Surface contract walkthrough step 2)
- **Observed**: `7 widgets` with exit status 0; `-5 widgets` with exit status 0.
- ✅ Both signed-integer invocations were walked at the reviewed HEAD and printed the unchanged text sentence with exit 0. `count.py:9` uses a bare `int(raw)` with no range check and `count.py:13` has no pluralization branch, so a negative value round-trips into the same sentence. The recorded isolated Verify in the task Log reports `test_positive_count (__main__.TextOutput) ... ok` and `test_negative_count (__main__.TextOutput) ... ok`.

### SC3 — `python3 count.py --json` prints exactly one JSON object, `{"widgets": 0}`, to stdout on its own and exits 0 — the `widgets` value is a JSON integer, not a string, and no human text line accompanies it.: pass

- **Surface**: count.py command line
- **Check**: `python3 count.py --json` with its exit status echoed, then `python3 count.py --json | od -c`, both run in the review sidecar worktree at 52bca9fe60bbdb72023c752470bb2d5366ed2dbf (PLAN.md Surface contract walkthrough step 3)
- **Observed**: stdout `{"widgets": 0}`, exit status 0; `od -c` shows `{   "   w   i   d   g   e   t   s   "   :       0   }  \n` — one object, one newline, no accompanying text line.
- ✅ The walkthrough at the reviewed HEAD produced exactly one JSON object and nothing else. `count.py:13` is the script's only `print`, a single ternary emitting either the JSON object or the text sentence, so a text line cannot accompany the object. `n` is bound by `int(raw)` on `count.py:9`, and the `od -c` bytes show an unquoted `0`, confirming a JSON integer rather than a string. `test_count.py:47-51` additionally asserts a single stdout line, payload equality with `{"widgets": 0}`, and `int` rather than `bool` type.

### SC4 — `python3 count.py --json 7` and `python3 count.py 7 --json` each print exactly one JSON object with `"widgets"` equal to the integer `7` and exit 0 — the flag is accepted before or after the optional count and both orders produce identical stdout.: pass

- **Surface**: count.py command line
- **Check**: `python3 count.py --json 7` and `python3 count.py 7 --json`, each with its exit status echoed, then the first piped into `cmp -s` against the second, all run in the review sidecar worktree at 52bca9fe60bbdb72023c752470bb2d5366ed2dbf (PLAN.md Surface contract walkthrough step 4)
- **Observed**: both orders printed `{"widgets": 7}` with exit status 0; the byte comparison reported `identical`.
- ✅ Both argument orders were walked at the reviewed HEAD and their stdout proved byte-identical under `cmp`. `count.py:6-7` detects the flag with a membership test over all of `sys.argv[1:]` and rebuilds the positional list by filtering every `--json` token, so the count read on `count.py:8` is position-independent — the naive `sys.argv[1]` regression flagged in INTENT.md Risks does not exist. Recorded isolated Verify: `test_json_flag_before_and_after_count_agree (__main__.JsonOutput) ... ok`.

### SC5 — `python3 count.py abc` writes a useful message naming the bad argument to stderr, writes nothing to stdout, exits nonzero, and prints no Python traceback; `python3 count.py --json abc` fails the same way rather than emitting a partial or malformed JSON object.: pass

- **Surface**: count.py command line
- **Check**: `python3 count.py abc` and `python3 count.py --json abc`, each with stdout and stderr redirected to separate capture files and the exit status echoed, then both captures printed, run in the review sidecar worktree at 52bca9fe60bbdb72023c752470bb2d5366ed2dbf (PLAN.md Surface contract walkthrough step 5)
- **Observed**: text mode exit status 2, stdout empty, stderr `count.py: not an integer: abc`; JSON mode exit status 2, stdout empty, stderr `count.py: not an integer: abc`. Neither stderr contained traceback text.
- ✅ Both bad-input walkthroughs at the reviewed HEAD exited nonzero with empty stdout and a one-line message naming the offending argument `abc`, and no `Traceback` appeared. The guard is structural rather than incidental: `count.py:8-12` resolves `int(raw)` inside `try`/`except ValueError` and calls `sys.exit(2)` from the handler before control can reach the single `print` on `count.py:13`, so a partial or malformed JSON object cannot be written in `--json` mode. Recorded isolated Verify: `test_text_mode_rejects_non_integer (__main__.BadArgument) ... ok` and `test_json_mode_rejects_non_integer_without_partial_json (__main__.BadArgument) ... ok`.

### SC6 — A stdlib-only test module in the repository launches the real CLI through `subprocess` and asserts criteria 1–5 against actual stdout, stderr, and exit status; running it reports every test passing.: pass

- ✅ `test_count.py` is added at the repository root by the isolated landing 426bd9f0447406d0a96d97815908cb277c3fa194. Its imports (`test_count.py:2-6`) are `json`, `os`, `subprocess`, `sys`, `unittest` — standard library only, with no manifest or runner introduced. `test_count.py:11-17` launches the real CLI with `subprocess.run([sys.executable, SCRIPT] + list(args), stdout=subprocess.PIPE, stderr=subprocess.PIPE, universal_newlines=True)`, where `SCRIPT` is `count.py` resolved from `__file__` on `test_count.py:8`, and every assertion reads `r.stdout`, `r.stderr`, or `r.returncode` rather than an imported function. Coverage of criteria 1–5 is complete: TextOutput covers 1–2, JsonOutput covers 3–4, BadArgument covers 5. The orchestrator's recorded isolated Verify in the task Log — `python3 test_count.py -v` in sidecar `gsd-path-verify/task-t001-verify` — reports pass, exit 0, all eight tests `ok`, ending `Ran 8 tests in 0.227s` / `OK`, so every test passes. This criterion is test-only and therefore carries no surface.

## Summary for orchestrator

- blocked findings: none. Every T001 acceptance criterion, the `None` interface contract, and all six owned INTENT success criteria pass against the isolated product at 52bca9fe60bbdb72023c752470bb2d5366ed2dbf, whose `count.py` and `test_count.py` are byte-identical to the T001 landing 426bd9f0447406d0a96d97815908cb277c3fa194 (the only later commit, 52bca9f, touches `.project/build/verify-ledger.jsonl` alone).
- repeat offenders: none — this is cycle 1 and no criterion has failed before.
- warnings worth a human eye: `test_json_output_carries_no_human_text_line` (`test_count.py:63-64`) is an assertion that cannot fail, so it adds no protection even though its criterion is proven elsewhere; the bad-input exit status `2` is unpinned by any criterion; and repeated or literal `--json` tokens are absorbed silently by the filter on `count.py:7` with no criterion governing them. None of the three defeats a criterion, so none blocks the wave.
