$gsd-path

Parent evaluator ruling, final recovery round under the owner's three-round MSW fuse: authorize returning T001 to pending, clearing its stale dispatch fields as orchestrator-owned bookkeeping, and preserving its exact product bytes and append-only logs. Preserve the product outside the repo; restore only those two product paths to their current committed baseline; transition build/blocked to build/active with the canonical expected-state helper; make the canonical .project-only recovery checkpoint including pending task bookkeeping. At that clean HEAD, create both task isolation and a canonical verification sidecar BEFORE activating T001 or restoring the product patch. Activate at that base, restore exact product hashes into primary and sidecar, verify their identical product diffs, run Task Verify once for this new base, append its result, and call canonical land directly at that base. Do not make any further checkpoint between activation and landing. Preserve the failure history. Continue full review and ship to local origin if all gates pass. If this round is blocked again, stop and report the open item rather than another repair cycle. This is evaluator authorization of the specific proposed recovery, not a new human receipt, and it does not authorize editing installed plugin source or manual STATE.md changes.
