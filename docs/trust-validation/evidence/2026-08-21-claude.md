# Dogfood — claude — 2026-08-21

- Verdict: **pass**
- Spawn API: Claude Code headless (`claude -p`), Agent tool for children
- Fixture: `<tmp>/repo`

| Check | Pass | Detail |
|---|---|---|
| install succeeded | yes | .md, .claude/CLAUDE.md, .gsd-path/guard_hook.py, .gsd-path/git_guard.py, .claude/settings.json, .git/hooks/pre-commit, . |
| skills installed locally | yes | .claude/skills |
| guard files present | yes | .gsd-path/, .git/hooks/ |
| host run exited 0 | yes | *Next** — Stopping here as instructed; no rulings collected, `## User rulings` is empty and nothing is queued. When you  |
| DOCS-AUDIT.md exists | yes | <tmp>/repo/.project/research/DOCS-AUDIT.md |
| Summary verdict table present | yes | ## Summary with counts |
| at least one doc section | yes | README.md, AGENTS.md, WORKFLOW.md, .project/STATE.md, .claude/CLAUDE.md |
| claims carry verdict + evidence | yes | 16 claim rows |
| aspirational --json claim detected (advisory) | yes | ['aspirational', 'stale', 'verified'] |
| pre-commit blocks archive modification | yes | gsd-path guard blocked the commit:
  M .project/archive/001-seed/NOTE.md: committed archives are read-only |
| commit outside archive succeeds | yes |  |

## Commands

- `node <repo>/scripts/install.mjs --claude --local --project <tmp>/repo --hooks --no-color` → exit 0
- `claude -p /gsd-path-docs-audit

Run the GSD Path docs audit for this repository in standalone mode. Write .project/research/DOCS-AUDIT.md, then stop. …` → exit 0

## Host output (tail)

```
**Outcome** — Docs audit complete, standalone mode at `define/done`. 5 docs audited (the 95 installed `.claude/skills/gsd-path*` files were excluded as vendored tooling and recorded as such). Verdicts: 9 verified, 5 stale, 1 aspirational, 0 unverifiable. Commands ran in an `isolation.py` verify sidecar at HEAD `4174046`, now retired; primary worktree untouched except the new audit file (uncommitted — the skill never commits).

Drift list:
1. `README.md` — `--json` flag is **aspirational**: `count.py` has no json handling; `python3 count.py 3 --json` prints `3 widgets`.
2. `AGENTS.md` — Distribution layout section is **stale** ×3: no `plugin.json`, no root `skills/`, no `scripts/sync_skill_resources.py`; skills live in `.claude/skills/`.
3. `WORKFLOW.md` — "each archive has MANIFEST.md" is **stale**: `archive/001-seed/` holds only `NOTE.md`.
4. `.project/STATE.md` — `define/done` is **stale**: no `intent/INTENT.md` exists (log says fixture state).

Remediation queue: 6 items — #2–#4 classified `fix-doc` (recommended: the AGENTS.md section describes the gsd-path source repo, not this install); #1, #5, #6 are `NEEDS-USER` (no evidence which side is intended). `discussion_records.py pending` → empty before and after.

**Review** — [DOCS-AUDIT.md](/private<tmp>/repo/.project/research/DOCS-AUDIT.md)
`/private<tmp>/repo/.project/research/DOCS-AUDIT.md`

**Next** — Stopping here as instructed; no rulings collected, `## User rulings` is empty and nothing is queued. When you want the ruling walk (fix-code / fix-doc / accept-drift per item, batches of three), invoke `/gsd-path-docs-audit` again or `/gsd-path`, which will offer the queue during planning.

```
