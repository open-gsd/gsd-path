# Dogfood — claude — 2026-08-21

- Verdict: **pass**
- Spawn API: Claude Code headless (`claude -p`), Agent tool for children
- Fixture: `<tmp>/repo`
- Note: verdicts seen: aspirational, stale, unverifiable, verified

| Check | Pass | Detail |
|---|---|---|
| install succeeded | yes | .md, .claude/CLAUDE.md, .gsd-path/guard_hook.py, .gsd-path/git_guard.py, .claude/settings.json, .git/hooks/pre-commit, . |
| skills installed locally | yes | .claude/skills |
| guard files present | yes | .gsd-path/, .git/hooks/ |
| host run exited 0 | yes | artifact is untracked (`?? .project/research/`); no commit was made. The remaining step in this skill is the ruling walk |
| DOCS-AUDIT.md exists | yes | <tmp>/repo/.project/research/DOCS-AUDIT.md |
| DOCS-AUDIT.md passes check_docs_audit.py | yes | {"alignment": true, "audit": ".project/research/DOCS-AUDIT.md", "claims": 16, "descriptive": [], "docs": [".claude/CLAUD |
| pre-commit blocks archive modification | yes | gsd-path guard blocked the commit:
  M .project/archive/001-seed/NOTE.md: committed archives are read-only |
| commit outside archive succeeds | yes |  |

## Commands

- `node <repo>/scripts/install.mjs --claude --local --project <tmp>/repo --hooks --no-color` → exit 0
- `claude -p /gsd-path-docs-audit

Run the GSD Path docs audit for this repository in standalone mode. Write .project/research/DOCS-AUDIT.md, then stop. …` → exit 0

## Host output (tail)

```
Gate passes. Per your instruction ("write DOCS-AUDIT.md, then stop"), I am stopping before the ruling walk; nothing was committed.

**Outcome**

Standalone docs audit done at `define/done` (legal boundary), HEAD `1f6b152`. Inventory: 5 docs (`.claude/skills/**` excluded as the installed gsd-path bundle — noted in the artifact). Project commands ran only in a verify sidecar, now retired; worktree list is back to primary only. Pending discussion receipts: none.

Verdicts: **11 verified, 5 stale, 0 aspirational, 0 unverifiable, 0 descriptive.** Bundled `check_docs_audit.py` gate: pass.

Drift list (remediation queue, all `NEEDS-USER`):
1. `README.md` — `--json` flag claimed; `python3 count.py --json` → `ValueError`. Never implemented.
2. `AGENTS.md` — `plugin.json` named in Distribution layout; absent.
3. `AGENTS.md` — `scripts/sync_skill_resources.py` sync command; absent.
4. `AGENTS.md` — `platforms/shared-agents/dispatch.md`; absent. (2–4 likely describe the upstream gsd-path source repo, not this project.)
5. `.project/STATE.md` — `archive: null` while `.project/archive/001-seed/` exists.

Process note, stated plainly: the auditor needed one corrective redispatch and still left mechanical gate defects (summary counts, a doc listed in both sections, an invalid verdict label, two dropped path rows). I fixed those as orchestrator with fresh `ls` evidence and recorded that in a note at the top of the artifact. No doc or code was changed.

**Review**

[DOCS-AUDIT.md](/private<tmp>/repo/.project/research/DOCS-AUDIT.md)
`/private<tmp>/repo/.project/research/DOCS-AUDIT.md`

**Next**

The artifact is untracked (`?? .project/research/`); no commit was made. The remaining step in this skill is the ruling walk (items 1–5, `fix-code` / `fix-doc` / `accept-drift`), which needs you present — re-invoke `/gsd-path-docs-audit` to run it, or `/gsd-path` will surface the queue at its next pass.

```
