# Dispatch driver E2E — 6024253

Date: 2026-09-07. **Completed unassisted through every gate.** Native Codex CLI
0.153.4, gpt-6-astra with high reasoning, quick lane, full review, no panel,
direct integration to a local origin: the same fixture and host settings as the
lean quick run `62f5e0f`. The evaluator approved intent, one define correction,
plan, and ship as the evaluation owner. No plugin file or pipeline metadata was
edited by hand.

## What this run proved

- The build phase ran through `dispatch_driver.py round --wave 1 --wait 540`
  in one call: the driver isolated T001 on the bound branch, spawned the coder
  through the owner's headless `codex exec --json` command, reproduced the
  patch in the pre-created verify sidecar, ran Verify there, appended the
  result to the task Log, landed `55e06ae`, recorded the ledger entry at that
  commit, retired the sidecar, and checkpointed bookkeeping as `43b12dc`.
- Full wave review, final preparation, a fresh final review, archive, ship
  `f2c014d`, direct integration `f0351d1`, and the milestone tag all passed
  through the canonical helpers. The primary is clean and no sidecar remains.

## Measured

| Measure | Lean quick run `62f5e0f` | Driver run `6024253` |
|---|---:|---:|
| Parent output tokens, all invocations | 25,355 | 25,854 |
| Parent output tokens, build invocation | 9,086 | 8,076 |
| Parent shell commands, build invocation | 50 | 34 |
| Build invocation seconds | 590 | 806 |
| Coder output tokens | 2,860 | 2,870 |
| Reviewer output tokens | 4,232 | 10,346 |
| Total native output tokens | 38,848 | 43,609 |
| Parent share of output | 65.3% | 59.3% |
| Invocations | 6 | 5 |
| Total CLI seconds | 1,531 | 1,638 |

Inside the build invocation the driver removed the hand-sequenced landing
steps: parent commands fell by a third and parent output by 11%. `build_state`
calls fell from four to zero, and the driver itself was one command.

The parent total did not fall, for two reasons that are not the driver:

1. The planner wrote integration as a sixth success criterion, which no task
   Verify can prove. The owner correction cost an extra invocation of 3,956
   parent tokens.
2. Final-review reuse was refused with "product or approved contracts changed
   after review". The parent had saved the landing proof as
   `.project/build/build-evidence.json`; the reuse rule allows only
   `.project/build/evidence.json`. The build contract said "retain its JSON
   receipt" without naming the path. A fresh final review followed: 3,322
   reviewer tokens, its parent dispatch, and about four minutes. This change
   makes `workflow_run.py build-evidence` write the canonical path itself.

What remains in the parent's build turn is review dispatch (sidecar, collect,
retire), review validation, eight state transitions, and final preparation.
Those are the next candidates for the driver.

## Also observed

- The coder child ran under `--sandbox workspace-write` and could not append
  the evaluator's activity file outside its worktree. It reported the failure
  and returned `RESULT: T001 ready`; the driver's own Verify and landing were
  unaffected.
- One observation on a one-task quick lane. This run does not establish how
  the driver's advantage changes with task count or parallel rounds.

## Evidence

- [Metrics, sessions, and intervals](evidence/releases/1.0.0/driver-e2e-6024253.json)
- Raw fixture, prompts, CLI events, driver records, and the integrated repository:
  `/Users/jeremymcspadden/orca/evaluations/gsd-path-driver-6024253`.
- Native parent thread `01a07c49-0287-7672-b07f-41ec4b4162ee`.
