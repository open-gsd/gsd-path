interface:
  display_name: "GSD Path Define"
  short_description: "Turn an idea into approved intent"
  default_prompt: "Use $gsd-path-define to define and approve this project's intent."
policy:
  allow_implicit_invocation: false
