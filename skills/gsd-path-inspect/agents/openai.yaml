interface:
  display_name: "GSD Path Inspect"
  short_description: "Establish existing-project ground truth"
  default_prompt: "Use $gsd-path-inspect to inspect this existing project before defining intent."
policy:
  allow_implicit_invocation: false
