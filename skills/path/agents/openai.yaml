interface:
  display_name: "path"
  short_description: "Route and resume the GSD Path pipeline"
  default_prompt: "Use $path to inspect project state and continue the next valid GSD Path phase."
policy:
  allow_implicit_invocation: false
