interface:
  display_name: "GSD Path Ship"
  short_description: "Verify, approve, archive, and ship"
  default_prompt: "Use $gsd-path-ship to verify the completed milestone and request final shipping approval."
policy:
  allow_implicit_invocation: false
